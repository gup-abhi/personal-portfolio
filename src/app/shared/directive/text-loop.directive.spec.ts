import { TextLoopDirective } from './text-loop.directive';

describe('TextLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new TextLoopDirective();
    expect(directive).toBeTruthy();
  });
});
