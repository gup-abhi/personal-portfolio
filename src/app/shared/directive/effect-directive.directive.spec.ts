import { EffectDirectiveDirective } from './effect-directive.directive';

describe('EffectDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EffectDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
